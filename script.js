// O modo claro/escuro automático é gerenciado puramente pelo CSS usando:
// @media (prefers-color-scheme: dark) { ... }
//
// O JavaScript abaixo é um placeholder caso você queira adicionar funcionalidades
// futuras (como um botão manual de troca de tema ou um contador de dias).

document.addEventListener('DOMContentLoaded', () => {
    console.log("Site carregado! Olá, Princesa!");
    
    // Exemplo de JS para um futuro botão de tema, se o usuário não quiser o modo automático:
    // const themeToggle = document.getElementById('toggle-theme');
    // if (themeToggle) {
    //     themeToggle.addEventListener('click', () => {
    //         document.body.classList.toggle('dark-mode');
    //     });
    // }
});
